# dotfiles-common
Shared dotfile snippets

## Usage

Source all aliases:

    source /path/to/dotfiles-common/source.sh

Or add to .zshrc:

    [ -f ~/dotfiles-common/source.sh ] && source ~/dotfiles-common/source.sh

